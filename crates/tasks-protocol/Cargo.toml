[package]
name = "tasks-protocol"
description = "Protocol types between the tasks server and Scout VMs"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde.workspace = true
# Promoted from a dev-dependency by `agent_run`, which reads the agent's own
# stream-json to classify how a run ended. Still a light dependency set: this
# crate is shared with the supervisors and must stay cheap to build in a VM.
serde_json.workspace = true
vm-pool-protocol.workspace = true

[dev-dependencies]
tempfile.workspace = true
